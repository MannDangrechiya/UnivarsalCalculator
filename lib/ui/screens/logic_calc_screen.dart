import 'package:flutter/material.dart';

class LogicCalcScreen extends StatelessWidget {
  const LogicCalcScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Logic Calculator')),
      body: const Center(child: Text('Logic Calculator UI here')),
    );
  }
}
